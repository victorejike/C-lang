#include <stdio.h>

int main(){
    // now i want to calculate the area of an rectangle 
    int  lenght = 60;
    int  width  = 40;
    int  area;

    area = lenght + width;
    printf("the lenght  is %d \n",lenght);
    printf("the width   is %d \n", width);
    printf("the total of the angle is %d \n",area);
    return 0;
}